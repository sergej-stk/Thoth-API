name: Services-Users-Pipeline
on:
  push:
    paths:
      - src/Services/Users/**
  pull_request:
    paths:
      - src/Services/Users/**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      - name: Restore dependencies
        run: dotnet restore src/Services/Users/UserAPI.csproj
      - name: Build
        run: dotnet build src/Services/Users/UserAPI.csproj --no-restore
      - name: Test
        run: dotnet test src/Services/Users/UserAPI.csproj --no-build --verbosity normal
